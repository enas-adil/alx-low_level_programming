#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("Size of a char:", sizeof(char)," byte(s)");
	sizeof(int);
	sizeof(long);
	sizeof(long long);
	sizeof(float);
	return (0);
}
